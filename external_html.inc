<h2>Including external HTML</h2>
<ul>
	<li>partial HTML can be inserted into any PHP file</li>
	<li>most partial HTML files end with <code>.inc</code> extension, but the extension does not really matter</li>
</ul>